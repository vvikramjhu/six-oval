#summary Web Services API, version 1.0.0.
#labels Phase-Implementation

= <font color="#0044CC">Introduction</font> =

In this document, we show the details
of SIX OVAL Web services.
In the latest version,
we provide only the _repository services_.



= <font color="#0044CC">REST API</font> =
T.B.D.

The resources in the OVAL domain are well-defined in 
[http://oval.mitre.org/language/ the OVAL Language specification].
Currently, XML is one and only 
standardized resource representation format.
Therefore, we adopt these resource model and representation.

Below, we describe the resource identifier design with URIs
and interaction design with HTTP requests and responses. 


= <font color="#0044CC">Repository Services</font> =

Suppose that the base URI of the service is
denoted as `$REPO_BASE`;
e.g. `http://foo.org/six-oval/repository`.


== <font color="#0044CC">Definition Resources</font> =

|| *Operation* || *HTTP Method* || *URI Path* || *Request Headers* || *Request Body* || *Response Headers* || *Response Body* ||
|| Find by ID || GET || `/definitions/{id}` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `oval-def:definition` ||
|| Find all || GET ||  `/definitions` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `six-oval:query_results` ||
|| Query || GET ||  `/definitions?{query}` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `six-oval:query_results` ||


== <font color="#0044CC">Element Resources: Definition, Test, Object, State, and Variable</font> =
T.B.D.


== <font color="#0044CC">Oval Definitions Document Resources</font> =

|| *Operation* || *HTTP Method* || *URI Path* || *Request Headers* || *Request Body* || *Response Headers* || *Response Body* ||
|| Find by ID || GET || `/oval_definitions/{id}` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `oval-def:oval_definitions` ||
|| Find all || GET ||  `/oval_definitions` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `six-oval:query_results` ||
|| Query || GET ||  `/oval_definitions?{query}` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `six-oval:query_results` ||
|| Create || POST || `/oval_definitions` || `Content-Type: application/xml` || `oval-def:oval_definitions` || `201 Created`<br>`Location: {URI}` || - ||


== <font color="#0044CC">Oval System Characteristics Document Resources</font> =
T.B.D.

|| *Operation* || *HTTP Method* || *URI Path* || *Request Headers* || *Request Body* || *Response Headers* || *Response Body* ||
|| Find by ID || GET || `/oval_scs/{id}` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `oval-sc:oval_system_characteristics` ||
|| Find all || GET ||  `/oval_scs` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `six-oval:query_results` ||
|| Query || GET ||  `/oval_scs?{query}` || `Accept: application/xml` || - || `200 OK`<br>`Content-Type: application/xml` || `six-oval:query_results` ||
|| Create || POST || `/oval_scs` || `Content-Type: application/xml` || `oval-sc:oval_system_characteristics` || `201 Created`<br>`Location: {URI}` || - ||


== <font color="#0044CC">Oval Results Document Resources</font> =
T.B.D.




--------